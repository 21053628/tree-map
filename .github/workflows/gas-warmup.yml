name: GAS Warm-up

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping GAS (follow redirects)
        run: |
          curl -sL -o /dev/null -w "最終狀態: HTTP %{http_code}｜耗時 %{time_total}s\n" \
            'https://script.google.com/macros/s/AKfycbxlwiMM1CW7tV4ljwU0GDJoM4CfDjqc7iS-yp65TzOX4DVsj4FD1BgVVlAhu2jegvn3/exec?action=ping' \
            || true
